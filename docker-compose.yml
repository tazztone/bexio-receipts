services:
  app:
    build: .
    volumes:
      - ./src/bexio_receipts:/app/src/bexio_receipts
      - ./src/bexio_receipts/templates:/app/src/bexio_receipts/templates
      - ./tests:/app/tests
      - ./inbox:/app/inbox
      - ./review_queue:/app/review_queue
      - ./processed_receipts.db:/app/processed_receipts.db
    environment:
      - BEXIO_API_TOKEN=${BEXIO_API_TOKEN:-test_token}
      - BEXIO_BASE_URL=http://mock-bexio:8001
      - REVIEW_PASSWORD=${REVIEW_PASSWORD:-admin}
      - SECRET_KEY=${SECRET_KEY:-super-secret-key-change-me-in-prod}
      - GLM_OCR_API_HOST=host.docker.internal
      - GLM_OCR_API_PORT=8080
      - GLM_OCR_LAYOUT_DEVICE=cpu
      - OLLAMA_URL=http://host.docker.internal:11434
      - LLM_PROVIDER=ollama
      - LLM_MODEL=qwen3.5
      - DEFAULT_BOOKING_ACCOUNT_ID=100
      - DEFAULT_BANK_ACCOUNT_ID=200
    ports:
      - "8000:8000"
    command: ["bexio-receipts", "serve", "--host", "0.0.0.0"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - mock-bexio

  watcher:
    build: .
    volumes:
      - ./src/bexio_receipts:/app/src/bexio_receipts
      - ./src/bexio_receipts/templates:/app/src/bexio_receipts/templates
      - ./tests:/app/tests
      - ./inbox:/app/inbox
      - ./review_queue:/app/review_queue
      - ./processed_receipts.db:/app/processed_receipts.db
    environment:
      - BEXIO_API_TOKEN=${BEXIO_API_TOKEN:-test_token}
      - BEXIO_BASE_URL=http://mock-bexio:8001
      - REVIEW_PASSWORD=${REVIEW_PASSWORD:-admin}
      - SECRET_KEY=${SECRET_KEY:-super-secret-key-change-me-in-prod}
      - GLM_OCR_API_HOST=host.docker.internal
      - GLM_OCR_API_PORT=8080
      - GLM_OCR_LAYOUT_DEVICE=cpu
      - OLLAMA_URL=http://host.docker.internal:11434
      - LLM_PROVIDER=ollama
      - LLM_MODEL=qwen3.5
      - DEFAULT_BOOKING_ACCOUNT_ID=100
      - DEFAULT_BANK_ACCOUNT_ID=200
    command: ["bexio-receipts", "watch", "folder"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - mock-bexio

  mock-bexio:
    build: .
    volumes:
      - ./src/bexio_receipts:/app/src/bexio_receipts
      - ./src/bexio_receipts/templates:/app/src/bexio_receipts/templates
      - ./tests:/app/tests
    ports:
      - "8001:8001"
    command: ["python", "tests/mock_bexio.py"]

#   glm-ocr-vllm:
#     image: vllm/vllm-openai:v0.19.0-ubuntu2404
#     command: >
#       vllm serve zai-org/GLM-OCR --port 8080
#       --served-model-name glm-ocr
#       --speculative-config '{"method":"mtp","num_speculative_tokens":3}'
#     ports: ["8080:8080"]
#     environment:
#       - HUGGING_FACE_HUB_TOKEN=${HUGGING_FACE_HUB_TOKEN}
#     volumes:
#       - hf_cache:/root/.cache/huggingface
#     deploy:
#       resources:
#         reservations:
#           devices:
#             - driver: nvidia
#               count: 1
#               capabilities: [gpu]

volumes:
# hf_cache:  # Uncomment when using glm-ocr-vllm service
